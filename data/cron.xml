<odoo>
    <data noupdate="1">
        <record id="ir_cron_subscription_sync" model="ir.cron">
            <field name="name">Synchronisation abonnements ANPR</field>
            <field name="model_id" ref="anpr_peage_subscription.model_anpr_subscription_pass"/>
            <field name="state">code</field>
            <field name="code">model.cron_sync_abonnements()</field>
            <field name="interval_type">minutes</field>
            <field name="interval_number">2</field>
            <!-- <field name="numbercall">-1</field> --> <!-- déjà supprimé -->
            <!-- <field name="doall">False</field> --> <!-- à supprimer aussi -->
            <field name="active">True</field>
        </record>
    </data>
</odoo>
